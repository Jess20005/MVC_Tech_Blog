<h2>Your Dashboard</h2>

{{#if posts.length}}
<div>
    {{#each posts as |post|}}
    <a href="{{post.id}}" <{{title}}"></a>
    {{/each}}
</div>

{{/if}}
<button type="button" class="btn btn-primary" href='#'>New Post</button>